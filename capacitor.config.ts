import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'controleTheoMaudet2BCI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
